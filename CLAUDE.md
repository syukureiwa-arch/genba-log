# 現場記録アプリ 引き継ぎ資料

このファイルと `index.html` をClaude Codeに渡せば、続きから作業できる。
プロジェクト直下に `CLAUDE.md` としてリネームして置くのを推奨。

---

## 1. これは何か

建設現場で働く職人が、その日の作業をスマホで記録するためのアプリ。
依存ライブラリゼロ・ビルド不要の単一HTMLファイル。ネットワーク通信は一切しない。
記録は端末内のIndexedDBに保存する（詳しくは4章）。

利用者は複数の工務店（元請）から仕事を受けており、同じ元請でも現場（場所）が頻繁に変わる。
そのため入力は「元請」「現場」の2階層構造になっている。

---

## 2. 現在の機能

### 記録する（入力タブ）
- 日付、開始時刻、終了時刻（`08:00〜17:00` などのワンタップ入力チップあり）
- 元請（工務店）※履歴チップ最大12件
- 現場（場所）※選択中の元請に紐づく現場のみチップ表示。元請未選択時は全現場を直近順＋元請名を併記
- 作業内容（履歴チップあり／14文字以下のもの最大8件）
- 備考（普段は折りたたみ。中身があるとボタンが色付きで「備考あり」表示に変わる）
- 材料（備考と同じ折りたたみ。品名／数量／単位／金額を1行ずつ積む。金額は空でよい）
  - 「＋ もう1行」で増やし、各行の × で消す
  - 品名と単位は過去の入力から履歴チップを出す。押すと**入力中の行**に入る
  - 中身があるとボタンが色付きの「材料3件（開く）」に変わる（備考と同じ挙動）
- 写真（1件あたり最大30枚。長辺900px・JPEG品質0.7に縮小してから保存）
- 入力の自動一時保存（5秒デバウンス。アプリを閉じても次回起動時に復帰）

### 見る（一覧タブ）

上部で表示を切り替える。

**元請から見る**（既定）… 3段の階層。

1. 元請の一覧（元請名・抱えている現場数・最終稼働日）
2. 現場の一覧（現場名・稼働日数・合計作業時間・最終稼働日。稼働日の新しい順）
3. 現場カルテ（下記）

戻るボタンで1段ずつ戻る。今どこにいるかはパンくずで出す。
この表示では月フィルタは隠す（全期間で見る。月別は現場カルテの中で出す）。

**日付順で見る** … 従来の一覧。書きかけのピン留めも月フィルタもそのまま。

検索は階層に関係なく横断で効く。**検索中は日付順の一覧に切り替わる**。
検索語を消すと元の段に戻る。

#### 現場カルテ
- 頭に現場名・元請・着手日〜最終稼働日
- 集計ボード（稼働日数／合計作業時間／材料の金額／写真枚数）。
  金額は全部空なら出さない
- 月ごとの稼働日数（2ヶ月以上にまたがる現場だけ出す）
- 材料。**ここで追加・編集・削除する**（打つと少し待ってから自動保存）
- 日々の記録を日付の新しい順。作業内容・備考・時間・写真。
  写真は一覧と同じ遅延読み込み
- 各記録から「続きを書く」で編集に飛べる

### 検索・絞り込み
- 月での絞り込み、フリーワード検索（元請・現場・作業内容・備考・**材料の品名**が対象）
  - 材料は現場の持ち物なので、品名が当たるとその現場の記録が全部出る
- 書きかけ（下書き）を最上部にピン留め表示
- 写真は画面に入ったぶんだけ遅延読み込み（IntersectionObserver）
- 材料は「材料 3件 ▾」と畳んで表示。タップで品名・数量・単位・金額が開く。
  金額が1つでも入っていれば小計も出す
- 写真タップで全画面ビューア（前後送り付き）
- 「続きを書く」で入力タブへ復帰、削除

### 集計タブ
- 記録日数／現場数／合計作業時間／写真枚数（選択中の月で絞られる）
- 元請ごとの日数・時間・現場数
- 現場ごとの日数・時間
- 現場ごとの材料（現場を選ぶと、品名ごとの数量合計と金額合計が出る。月フィルタが効く）
- テキスト書き出し（選択中の月のみ）
- バックアップ保存／復元（JSON、写真込み）。最終バックアップ日を表示
- バックアップの催促（下記）
- 改善候補リストと自由記入メモ欄

### バックアップの催促
- 最後のバックアップから **14日** 経つと、画面上部に帯を出す
- 一度もバックアップしていない時は、使い始めた日を基準にする
- 記録が1件も無ければ出さない
- 帯の「今すぐ保存」でその場でバックアップできる。「後で」で **3日** 黙る
- バックアップを取ると催促は消え、「後で」も解除される
- 日数のしきい値は `BACKUP_DAYS` と `SNOOZE_DAYS` で変えられる

> 実際にファイルを保存し切ったかまではブラウザからは分からないので、
> **書き出せた時点**で「バックアップした」と見なしている。
> 保存ダイアログをキャンセルされた場合は取れていないのに催促が消える。

---

## 3. データ構造

### 記録本体（キー: `genba_logs_v2`）
JSON配列。1件は以下の形。

```js
{
  id: "1756800000000-a1b2c",  // Date.now()+ランダム
  date: "2026-09-01",          // YYYY-MM-DD
  client: "○○工務店",          // 元請。空文字あり
  site: "田中様邸",             // 現場。空文字あり
  work: "3F 型枠建込み",        // 作業内容
  note: "施主立会いあり",        // 備考
  start: "08:00",              // HH:MM（2桁必須。1桁だとinput[type=time]が受け付けない）
  end: "17:00",
  photoCount: 5,               // 写真の枚数のみ保持。実体は別キー
  c: 1,                        // チャンク形式で保存済みの印
  draft: false                 // 下書き判定は5章を見ること
}
```

### 写真（キー: `genba_photo:<id>:<連番>`）
- base64のdataURL文字列の配列を、**3枚ずつ**に分けて別キーに保存
- 記録本体に写真を含めない設計。含めると1キーの容量上限を超えて保存が丸ごと失敗する
- 読み出しは連番0から順に、キーが無くなるまで

### 現場の材料（キー: `genba_site_materials`）

**材料は現場の持ち物**。記録（日報）には付いていない。
現場を識別するキーは 元請名 + `\u001f` + 現場名（どちらも空文字があり得る）。

```js
{
  "山田工務店\u001f田中様邸": [
    { name:"杉板", qty:"10", unit:"枚", price:"12000",
      date:"2026-09-01",   // 入れた日。空でよい
      from:"1756800000000-a1b2c", // 入れた記録のID。カルテで直接足した行は空
      gone:"" }            // "1" なら、入れた元の日報は削除済み
  ]
}
```

- `from` があるので、記録を編集し直しても材料が二重に増えない
  （保存のたびに、その記録から入れた行を入れ替える）
- 記録の現場を変えて保存すると、材料も新しい現場へ移る
- **記録を消しても材料は消えない。** 実際に現場へ届いたものなので、日報の
  削除に巻き込まない。代わりに `gone:"1"` を立てて `from` を外し、現場に
  直接足した行と同じ扱いにする。カルテでは行の下に「元の日報は削除済み」と出る

### 保存先

IndexedDB（DB名 `genba-log` / オブジェクトストア `kv`）にキーと値で入れている。
値は文字列。キー名は下記のまま変えていない。

### その他のキー
- `genba_draft` … 入力中の一時保存
- `genba_draft_photos` … 一時保存の写真（先頭6枚まで）
- `genba_notes` … 集計タブの自由メモ
- `genba_migrated` … 旧保存先からの取り込み済みの印（初回起動時に1回だけ動く）
- `genba_mat_moved` … 材料を記録から現場へ移し終えた印
- `genba_premigrate_logs` … 材料を移す直前の記録の控え（**消さずに残してある**）
- `genba_backup_at` … 最後にバックアップを書き出した時刻
- `genba_backup_snooze` … 催促で「後で」を押した時、いつまで黙るか
- `genba_first_use` … 使い始めた時刻。一度もバックアップが無い時の催促の基準

---

## 4. 保存が消える問題（保存層の差し替え：完了）

### 何が起きていたか

旧 `store` は起動時に3段階で保存先を探していた。

1. `window.storage`（Claudeのアーティファクト用ストレージ）
2. `localStorage`
3. メモリ（閉じたら消える）

- `window.storage` はアーティファクトを「公開」した状態でしか動作せず、作成・テスト中は
  `Storage set failed: Unexpected response type` で失敗していた
- そのため実際には `localStorage` にフォールバックしていたが、**新バージョンを配布するたびに
  保存領域が別扱いになり、記録が参照できなくなった**（実際にデータ消失が発生済み）

### いまどうなっているか

保存先は **IndexedDB**（DB名 `genba-log` / オブジェクトストア `kv`）。キーと値の形は今までと同じで、
キー名（`genba_logs_v2`、`genba_photo:<id>:<連番>` など）も据え置き。値は文字列のまま入れている。

`store.get / set / del` の呼び出し形は変えていないので、呼び出し側のコードはそのまま動く。
`get` は今まで通り `{key,value}` か `null` を返す。

保存先の決まり方（`store.mode`）は次の3段階。

| mode | 中身 | 画面上の表示 |
|---|---|---|
| `idb` | IndexedDB。本命 | 帯は出ない |
| `local` | localStorage。IndexedDBが使えない時だけ | 黄色い帯で理由を表示 |
| `mem` | どちらも駄目。閉じると消える | 赤い帯で警告 |

IndexedDBの判定は「開く→試し書き→読み直し」まで確認して初めて `idb` にする。
プライベートモードなどで `open` が返ってこない環境に備えて5秒で打ち切る。

### 旧データの救出（初回起動時に1回だけ）

`store.rescue()` が、旧バージョンが残した記録をIndexedDBへ引き取る。

- `localStorage` にある `genba` で始まるキーを全部さらう
- `window.storage` は一覧が取れないので、判っているキー名だけ順に当たる
  （記録本体を読んでから、その記録IDの写真チャンクを連番でたどる）
- **IndexedDB側に既にあるキーは上書きしない**
- **元データは消さない。** 取り込みに失敗しても手元に残るように
- 済んだ印を `genba_migrated` に残すので、2回目以降は走らない

取り込みが起きた回だけ、画面上部に「前の保存先に残っていた◯項目を取り込みました」と出る。

### バックアップJSONの取り込み

「集計」→「バックアップから復元」。受け付ける形は2通り。

- `{v:2, logs:[...], photos:{id:[dataURL,...]}}` … 現行の書き出し形式
- 記録だけの配列 … 古い書き出し

同じIDの記録は飛ばして追加する（重複しない）。ファイルが壊れている・形が違う場合は
取り込む前に止めるので、今ある記録には触らない。写真が入らなかった記録も、記録本体は残す。

### PWA化（済）

ホーム画面に追加して、オフラインで起動できる。追加したファイルは次の通り。

| ファイル | 役割 |
|---|---|
| `manifest.json` | アプリ名・アイコン・standalone表示。ホーム画面追加に必要 |
| `sw.js` | Service Worker。オフライン起動 |
| `icon-192.png` / `icon-512.png` | ホーム画面のアイコン |
| `icon.svg` | アイコンの元データ。作り直す時はこれを編集して書き出す |
| `sw.test.js` | `sw.js` の動作確認（`node sw.test.js`） |

`index.html` 側には manifest への link、`theme-color`、apple-touch-icon、
それと `sw.js` の登録を足しただけ。**登録に失敗しても握り潰して普通に動く**ので、
`file://` で直接開いた時も今まで通り使える。

取り出し方は **stale-while-revalidate**。

1. キャッシュにあれば、まずそれを返す（電波が悪くても即起動する）
2. 裏で取り直して、キャッシュを更新する

現場は電波が悪いことが多いので、起動速度を優先してこうしてある。
代わりに、**アプリを更新すると反映は次の起動から**になる。
一度開いて閉じ、もう一度開くと新しい版になる。

> **アプリを更新したら `sw.js` の `VERSION` を上げること。**
> 上げないと古いキャッシュが残り続ける。`activate` で古い版だけ消える。

アイコンは maskable 対応。Androidが丸や角丸に切り抜いても欠けないよう、
絵柄は中心から半径204px（＝40%）の安全圏に収めてある。`icon.svg` を触る時はここを崩さないこと。

### 配信先（済）

https://syukureiwa-arch.github.io/genba-log/

GitHub Pages（`main` ブランチの `/` を配信）。`git push` すれば1〜2分で反映される。

**アプリを更新する時は `sw.js` の `VERSION` を上げてから push すること。**
上げないと古いキャッシュが残り、いつまでも新しい版にならない。

### 実配信での確認結果

上記URLで確認済み。

- Service Worker が登録され `activated` まで進む（scope は `/genba-log/`）
- プリキャッシュに5点すべて入る（`/`、`index.html`、`manifest.json`、アイコン2点）
- **サーバに存在しないパス** `/genba-log/no-such-page-xyz` を開くと、GitHubの404ではなく
  キャッシュされたアプリが返る。つまり Service Worker が実際に通信を肩代わりしている
  ＝電波が無くても起動する
- 記録を保存して再読込しても残る（IndexedDB）

### 実機での確認（すべて確認済み）

利用者本人のスマホで、2026-09-02 に次の2点を確認済み。

- ホーム画面に追加したアイコンから起動できる
- **機内モードでもアイコンから起動できる**（Service Worker のキャッシュが効いている）

**これで当初の「保存が消える」問題は完全に閉じた。**

- 保存先が IndexedDB になり、localStorage の容量上限から外れた
- オリジンが `syukureiwa-arch.github.io` に固定されたので、
  アプリを更新しても記録は残る（配布のたびに保存領域が変わる問題が消えた）
- 電波の無い現場でも起動して記録できる

写真は今もdataURL文字列で持っている。IndexedDBはBlobをそのまま置けるので、
落ち着いたら差し替えると容量効率が良くなる（表示・書き出し側も直す必要がある）。

---

## 5. 設計上の決定事項（変える時は理由を確認すること）

- **材料は現場の持ち物。記録には持たせない。** 現場のキーは 元請名＋現場名。
  読む時は `rowsOf(client,site)`、外から来たものは `normMats()` で形を揃える
- **記録タブから入れた材料には `from`（記録のID）を必ず付ける。**
  `attachRows()` が `from` と入れた日を上書きするので、編集し直しても二重にならない。
  入力欄の行は `from:''` を持っていて `normMats()` の既定値では埋まらないため、
  ここは既定値任せにしないこと
- **数量と金額は入力されたままの文字列で持つ。** 「少々」のような書き方を殺さないため。
  集計する時だけ `numOf()` で数として読む
- **下書き判定は「現場があり、かつ 作業内容か材料のどちらかがある」。**
  材料だけ届く日が稀にあるので、それを下書き扱いにしない（`isDraft()`）
- **同じ品名でも単位が違えば合算しない。** 「杉板10枚」と「杉板2本」は別行で出す
- **月で絞ると、入れた日が空の材料は集計から外れる。** 月に振り分けられないため
- **記録を消しても材料は残す。** 材料は現実に現場へ届いたもので、日報の削除に
  巻き込まれるのは不自然。印（`gone`）を付けて、由来が追えなくなったことだけ示す
- **写真は記録本体と必ず分離する。** 一緒にすると1件の書き込みが肥大化して、保存が全部道連れになる
- **保存失敗時に入力内容を消さない。** 失敗したら理由を画面に赤字で出し、フォームはそのまま残す
- **書き込み回数を抑える。** 自動保存は5秒デバウンス＋前回と同一内容なら書き込まない。写真は変更が無ければ書き込まない
- **屋外での視認性を優先。** 文字は大きめ、タップ領域は広め、コントラスト高め。`user-scalable=no` は付けない（拡大できないと困る）
- **HTMLの組み立ては必ずエスケープを通す。** `esc()` を使用。インラインの `onclick` は使わずイベント委譲で処理する
- 見た目はモルタル色＋こて青の配色。派手にしない
- **記録そのものをリポジトリに置かない。** リポジトリは公開されている。
  アプリが書き出すバックアップJSON（`現場記録バックアップ_*.json`）とテキスト
  （`現場記録_*.txt`）、写真、`.env` の類は `.gitignore` で弾いてある。
  退避したい時は `backups/` など無視される場所へ置くこと。
  **`git add .` を打っても入らない**ことは確認済み

---

## 6. 既知の未実装・改善候補

- 写真1枚ごとの一言メモ（30枚撮ると後から判別できない）
- 終わった現場を履歴チップから手動でしまう機能
- 前日分をあとから入力する導線（日付変更が地味に面倒）
- 元請・現場の名称を一括で修正する機能（打ち間違いが履歴に残り続ける）

---

## 7. 移行手順の想定

1. 現行アプリからバックアップJSONを吸い出す（可能であれば）
2. ~~プロジェクトフォルダを作り、gitで管理を開始する~~ ← **済**
3. ~~`index.html` の保存層をIndexedDBに差し替える~~ ← **済**
4. ~~バックアップの取り込みで既存データを復旧、動作確認~~ ← **済**
5. ~~PWA化（manifest / Service Worker）~~ ← **済**
6. ~~静的ホスティングにデプロイ、スマホでホーム画面に追加~~ ← **済**
7. 落ち着いてからファイル分割やリファクタに着手する ← **次はここ**

データの安全確保が最優先で、構造の綺麗さは後回しで良い。

### 動作確認のしかた

`file://` で直接開くとIndexedDBやService Workerの挙動が本番と変わるので、確認は必ず
`http://` 経由で行う。同梱の開発用サーバを使う。

```
node dev-server.js
```

`http://localhost:8777/` で開く。Node以外の依存はない。

Service Worker のロジックだけは、ブラウザを立てずに確かめられる。

```
node sw.test.js
```

### 材料の移行を元に戻したい時

移行の直前の記録が `genba_premigrate_logs` に丸ごと残してある。
ブラウザの開発者ツールから次を実行すると、材料が付いた状態の記録に戻せる。

```js
logs = JSON.parse((await store.get('genba_premigrate_logs')).value);
await saveLogs();
await store.del('genba_mat_moved');      // 次の起動でまた移行が走る
await store.del('genba_site_materials'); // 現場側を消す場合
```

### 元に戻したい時

gitの履歴に入っている。差し替え前のHTMLは最初のコミットにある。

```
git show 4c494ef:genba-log.html > /tmp/old.html
```

（`4c494ef` = 「差し替え前の現場記録アプリを取り込む」）
